node_modules/
dist/
*.log
.DS_Store
*.tsbuildinfo

# Secrets — never commit
backend/.env

# Local database
backend/data/*.db
backend/data/*.db-journal
.gstack/

# Rapports générés (npm run rapport) — reconstructibles, ~300 Ko chacun
portefeuille/rapport-*.html

# Données perso (avoirs / historique de transactions) — dépôt PUBLIC, ne jamais committer
/Historique-*.xlsx
frontend/src/data/comptes/

# DUO.MOM (#77) — courbe backtest LOCALE, non validée, ne jamais publier
frontend/src/data/duo-mom.json
